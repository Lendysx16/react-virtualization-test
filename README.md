# React lists with virtualization

## Libraries

* React
* Redux toolkit/query
* react-intersection observer
* JSON-server for mock API

## Installation

1. Clone this repo
2. In therminal run following commands:

   ```bash
   npm install
   ```

   ```bash
   npm run serve
   ```

    Serve command starts react application along with json-server.
